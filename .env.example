# Postgres connection (matches docker-compose.yml)
DB_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Subsquid Portal dataset (Ethereum mainnet)
PORTAL_URL=https://portal.sqd.dev/datasets/ethereum-mainnet

# Ethereum JSON-RPC endpoint — used ONLY by verification scripts (eth_getStorageAt / balanceOf)
RPC_URL=https://eth.llamarpc.com
